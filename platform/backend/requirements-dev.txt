# Development/documentation dependencies (not needed in production)
-r requirements.txt
matplotlib>=3.10
mkdocs==1.6.1
mkdocs-material==9.7.4
pymdown-extensions==10.8

# Test harness (pytest suite under tests/, config in pytest.ini)
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.26.0
# Postgres driver for the ephemeral test database (same pin as requirements.txt)
psycopg2-binary==2.9.9
