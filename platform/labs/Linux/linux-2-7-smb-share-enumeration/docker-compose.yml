services:
  target:
    mem_limit: 512m
    cpus: 0.5
    build:
      context: ./containers/target
      dockerfile: Dockerfile
    hostname: blackwell-fs01
    labels:
      ip_offset: "22"
    restart: unless-stopped
