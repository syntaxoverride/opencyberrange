
services:
  webserver:
    mem_limit: 512m
    cpus: 0.5
    build:
      context: ./containers/webserver
      dockerfile: Dockerfile
    hostname: api-webserver
    labels:
      ip_offset: "47"
    restart: unless-stopped

