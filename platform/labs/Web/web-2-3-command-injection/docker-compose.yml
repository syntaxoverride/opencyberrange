services:
  webapp:
    mem_limit: 512m
    cpus: 0.5
    build:
      context: ./containers/webapp
      dockerfile: Dockerfile
    hostname: cloudping-app
    labels:
      ip_offset: "18"
    restart: unless-stopped
